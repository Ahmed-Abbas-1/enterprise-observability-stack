input {
  tcp {
    port => 5000
    codec => json
  }
  gelf {
    port => 12201
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "enterprise-logs-%{+YYYY.MM.DD}"
  }
  stdout { codec => rubydebug }
}
